-- Documentation of MongoDB indexes

-- Equivalent MongoDB commands:

-- db.options_chain.createIndex({ strike: 1 })
-- db.options_chain.createIndex({ datetime: 1 })
-- db.options_chain.createIndex({ expiry: 1 })